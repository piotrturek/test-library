<div class="row">
  <div class="col-lg-6 col-xs-12">
  
    <%= form_for(library_book) do |f| %>
      <% if library_book.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(library_book.errors.count, "error") %> prohibited this library_book from being saved:</h2>

          <ul>
          <% library_book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, :class => 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :lead %>
        <%= f.text_area :lead, :class => 'form-control' %>
      </div>

      <div class="form-check">
        <%= f.check_box :is_active, :class => 'form-check-input' %>
        <%= f.label :is_active %>
      </div>

      <div class="actions">
        <%= f.submit "Zapisz", class: 'btn btn-primary' %>
      </div>
    <% end %>

  </div>
</div>

